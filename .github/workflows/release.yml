name: release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to build and publish (e.g. v0.4.1)"
        required: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          # On a release event, github.ref is the tag, so checkout uses it by
          # default. On manual dispatch we leave this empty so checkout falls
          # back to the branch HEAD (which carries the release tooling); older
          # tags may predate scripts/check_release_version.py.
          ref: ${{ github.event.release.tag_name }}
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Verify version matches release tag
        run: python scripts/check_release_version.py "${{ github.event.release.tag_name || github.event.inputs.tag }}"
      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade build
          python -m build
      - name: Check artifacts
        run: |
          python -m pip install --upgrade twine
          python -m twine check dist/*
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    if: vars.PUBLISH_TO_PYPI == 'true'
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

# Publishing is opt-in. Set the repository variable PUBLISH_TO_PYPI to "true"
# (Settings > Secrets and variables > Actions > Variables) once a Trusted
# Publisher is registered on PyPI for this project. Until then a release
# builds and verifies the artifact but does not push it anywhere.
