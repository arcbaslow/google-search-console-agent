google-api-python-client>=2.100,<3
google-auth>=2.25,<3
google-auth-oauthlib>=1.2,<2
jinja2>=3.1,<4

# weasyprint is an optional PDF-rendering dependency. Markdown is the
# default audit output and needs no extra system libs. Install via the
# pdf extras when you need PDF:
#     pip install -e ".[pdf]"
# Or just:
#     pip install weasyprint
